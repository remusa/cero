type User {
    id: ID! @unique
    name: String!
    email: String! @unique
    password: String!
    resetToken: String
    resetTokenExpiry: Float
}

type Fast {
    id: ID! @unique
    startDate: DateTime!
    endDate: DateTime
    isActive: Boolean @default(value: "true")
    user: User!
}
